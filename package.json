{
  "name": "dynamic-import-test",
  "version": "0.0.0",
  "devDependencies": {
    "wrangler": "2.20.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --no-bundle",
    "deploy": "wrangler publish --no-bundle"
  }
}
